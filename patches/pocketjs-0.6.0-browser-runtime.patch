diff --git a/core/src/raster.rs b/core/src/raster.rs
index ee8105a..1ef8a62 100644
--- a/core/src/raster.rs
+++ b/core/src/raster.rs
@@ -144,0 +145,17 @@ pub fn render_scaled(ui: &Ui, words: &[u32], fb: &mut [u8], scale: u32) {
+    render_viewport_scaled(ui, words, fb, SCREEN_W, SCREEN_H, scale);
+}
+
+/// Execute `words` into an arbitrary logical viewport at 1× density.
+pub fn render_viewport(ui: &Ui, words: &[u32], fb: &mut [u8], width: u32, height: u32) {
+    render_viewport_scaled(ui, words, fb, width, height, 1);
+}
+
+/// Execute `words` into an arbitrary logical viewport at integer density.
+pub fn render_viewport_scaled(
+    ui: &Ui,
+    words: &[u32],
+    fb: &mut [u8],
+    width: u32,
+    height: u32,
+    scale: u32,
+) {
@@ -148,0 +166 @@ pub fn render_scaled(ui: &Ui, words: &[u32], fb: &mut [u8], scale: u32) {
+    assert!(width > 0 && height > 0, "viewport dimensions must be positive");
@@ -150,2 +168,2 @@ pub fn render_scaled(ui: &Ui, words: &[u32], fb: &mut [u8], scale: u32) {
-    let width = SCREEN_W as i32 * scale;
-    let height = SCREEN_H as i32 * scale;
+    let width = width as i32 * scale;
+    let height = height as i32 * scale;
diff --git a/host-web/engine.js b/host-web/engine.js
index 7dd794b..82808de 100644
--- a/host-web/engine.js
+++ b/host-web/engine.js
@@ -8 +8 @@
-// putImageData onto a 480x272 canvas (CSS-scaled, pixelated).
+// putImageData onto a window-sized canvas.
@@ -86,0 +87 @@ let held = 0;
+let pointerPressed = 0;
@@ -90,0 +92,3 @@ let frameCb = null;
+let resizeObserver = null;
+let renderScale = 1;
+let rasterDensity = 1;
@@ -102,0 +107 @@ let hudMem = 0;
+let hudEnabled = true;
@@ -166,2 +171,2 @@ function devtoolsScreenshot() {
-  shot.width = FB_W;
-  shot.height = FB_H;
+  shot.width = wasm.viewport.w;
+  shot.height = wasm.viewport.h;
@@ -169 +174 @@ function devtoolsScreenshot() {
-  const img = sctx.createImageData(FB_W, FB_H);
+  const img = sctx.createImageData(wasm.viewport.w, wasm.viewport.h);
@@ -201 +206,3 @@ function safeFrame() {
-    frameCb(held, packedAnalog());
+    const buttons = held | pointerPressed;
+    pointerPressed = 0;
+    frameCb(buttons, packedAnalog());
@@ -212 +219 @@ function blit() {
-  imageData.data.set(wasm.render());
+  imageData.data.set(renderScale === 1 ? wasm.render() : wasm.renderScaled(renderScale));
@@ -214 +221,6 @@ function blit() {
-  drawHud(ctx, FB_W, FB_H, hudFps, hudMem); // built-in on-canvas overlay
+  if (hudEnabled) {
+    ctx.save();
+    ctx.scale(renderScale, renderScale);
+    drawHud(ctx, wasm.viewport.w, wasm.viewport.h, hudFps, hudMem);
+    ctx.restore();
+  }
@@ -280,0 +293,42 @@ export function pressVirtual(bit, down) {
+// Browser physical-pointer bridge. The app owns cursor mode and hit-testing;
+// the host only converts CSS canvas coordinates into the logical viewport.
+function forwardPointer(event) {
+  if (!canvas || typeof globalThis.__pocketPointerMove !== "function") return false;
+  const rect = canvas.getBoundingClientRect();
+  if (rect.width <= 0 || rect.height <= 0) return false;
+  if (event.clientX < rect.left || event.clientX >= rect.right
+    || event.clientY < rect.top || event.clientY >= rect.bottom) return false;
+  const width = wasm?.viewport.w ?? FB_W;
+  const height = wasm?.viewport.h ?? FB_H;
+  const x = ((event.clientX - rect.left) * width) / rect.width;
+  const y = ((event.clientY - rect.top) * height) / rect.height;
+  globalThis.__pocketPointerMove(x, y);
+  return true;
+}
+
+function onPointerMove(event) {
+  forwardPointer(event);
+  if (canvas) canvas.style.cursor = "none";
+}
+
+function onPointerLeave() {
+  if (canvas) canvas.style.cursor = "";
+  globalThis.__pocketPointerLeave?.();
+}
+
+function onPointerDown(event) {
+  if (event.button !== 0) return;
+  forwardPointer(event);
+  held |= BTN.CIRCLE;
+  pointerPressed |= BTN.CIRCLE;
+  canvas?.setPointerCapture?.(event.pointerId);
+  event.preventDefault();
+}
+
+function onPointerUp(event) {
+  if (event.button !== 0) return;
+  if (!forwardPointer(event)) globalThis.__pocketPointerLeave?.();
+  held &= ~BTN.CIRCLE;
+  event.preventDefault();
+}
+
@@ -282,0 +337,21 @@ export function pressVirtual(bit, down) {
+function resizeCanvas() {
+  if (!canvas || !ctx || !wasm) return;
+  const rect = canvas.getBoundingClientRect();
+  const width = Math.max(1, Math.min(8192, Math.round(rect.width)));
+  const height = Math.max(1, Math.min(8192, Math.round(rect.height)));
+  const deviceScale = Number.isFinite(window.devicePixelRatio) ? window.devicePixelRatio : 1;
+  const maximumScale = Math.max(1, Math.min(4, Math.floor(8192 / width), Math.floor(8192 / height)));
+  const nextRenderScale = Math.max(1, Math.min(Math.ceil(deviceScale), maximumScale));
+  const physicalWidth = width * nextRenderScale;
+  const physicalHeight = height * nextRenderScale;
+  if (canvas.width === physicalWidth && canvas.height === physicalHeight
+    && wasm.viewport.w === width && wasm.viewport.h === height
+    && renderScale === nextRenderScale) return;
+  renderScale = nextRenderScale;
+  canvas.width = physicalWidth;
+  canvas.height = physicalHeight;
+  ctx.imageSmoothingEnabled = false;
+  imageData = ctx.createImageData(physicalWidth, physicalHeight);
+  wasm.resize(width, height);
+}
+
@@ -286,0 +362,6 @@ export async function mount(theCanvas, opts = {}) {
+  const requestedRasterDensity = opts.rasterDensity ?? 1;
+  if (!Number.isInteger(requestedRasterDensity) || requestedRasterDensity < 1 || requestedRasterDensity > 4) {
+    throw new RangeError(`rasterDensity must be an integer from 1 through 4, got ${requestedRasterDensity}`);
+  }
+  rasterDensity = requestedRasterDensity;
+  hudEnabled = opts.hud !== false;
@@ -288,2 +368,0 @@ export async function mount(theCanvas, opts = {}) {
-  canvas.width = FB_W;
-  canvas.height = FB_H;
@@ -292 +370,0 @@ export async function mount(theCanvas, opts = {}) {
-  imageData = ctx.createImageData(FB_W, FB_H);
@@ -296,0 +375 @@ export async function mount(theCanvas, opts = {}) {
+    pointerPressed = 0;
@@ -298,0 +378,5 @@ export async function mount(theCanvas, opts = {}) {
+  canvas.addEventListener("pointermove", onPointerMove);
+  canvas.addEventListener("pointerleave", onPointerLeave);
+  canvas.addEventListener("pointerdown", onPointerDown);
+  window.addEventListener("pointerup", onPointerUp);
+  canvas.addEventListener("contextmenu", (event) => event.preventDefault());
@@ -301 +385 @@ export async function mount(theCanvas, opts = {}) {
-  const res = await fetch("pocketjs.wasm");
+  const res = await fetch(new URL("pocketjs.wasm", import.meta.url));
@@ -304 +388,6 @@ export async function mount(theCanvas, opts = {}) {
-  connectDevtools();
+  resizeCanvas();
+  resizeObserver?.disconnect();
+  resizeObserver = new ResizeObserver(resizeCanvas);
+  resizeObserver.observe(canvas);
+  window.addEventListener("resize", resizeCanvas);
+  if (opts.devtools !== false) connectDevtools();
@@ -317 +406 @@ export async function load(name, opts = {}) {
-  wasm.init(); // fresh Ui: tree/styles/atlases/textures all reset
+  wasm.init(rasterDensity); // fresh Ui: tree/styles/atlases/textures all reset
@@ -331 +420 @@ export async function load(name, opts = {}) {
-    const pak = await fetch("dist/" + name + ".pak");
+    const pak = await fetch(new URL("dist/" + name + ".pak", import.meta.url));
@@ -333 +422 @@ export async function load(name, opts = {}) {
-    const srcRes = await fetch("dist/" + name + ".js");
+    const srcRes = await fetch(new URL("dist/" + name + ".js", import.meta.url));
diff --git a/host-web/index.html b/host-web/index.html
index 4a26a16..e55d6bf 100644
--- a/host-web/index.html
+++ b/host-web/index.html
@@ -2,2 +1,0 @@
-<!-- PocketJS dev host: 480x272 canvas playground, demo picker, virtual buttons.
-     Serve with: bun host-web/serve.ts   (or bun scripts/dev.ts) -->
@@ -7 +5 @@
-<title>PocketJS playground</title>
+<title>Control Island</title>
@@ -11,4 +9,6 @@
-  body {
-    margin: 0; padding: 24px; background: #0b0e17; color: #cbd5e1;
-    font: 14px/1.5 ui-monospace, SFMono-Regular, Menlo, monospace;
-    display: flex; flex-direction: column; align-items: center; gap: 14px;
+  html, body {
+    width: 100%;
+    height: 100%;
+    margin: 0;
+    overflow: hidden;
+    background: #020617;
@@ -16 +15,0 @@
-  h1 { font-size: 16px; margin: 0; color: #e2e8f0; }
@@ -18,17 +17,4 @@
-    width: 960px; height: 544px; /* 2x logical 480x272 */
-    max-width: 100%;
-    image-rendering: pixelated;
-    background: #000; border: 1px solid #1e293b; border-radius: 6px;
-  }
-  .row { display: flex; gap: 10px; align-items: center; flex-wrap: wrap; justify-content: center; }
-  select, button {
-    background: #1e293b; color: #e2e8f0; border: 1px solid #334155;
-    border-radius: 6px; padding: 6px 12px; font: inherit; cursor: pointer;
-  }
-  button:active, button.held { background: #6366f1; border-color: #818cf8; }
-  .pad button { min-width: 44px; }
-  #keys { color: #64748b; font-size: 12px; text-align: center; }
-  #log {
-    width: 960px; max-width: 100%; height: 90px; overflow-y: auto;
-    background: #0f172a; border: 1px solid #1e293b; border-radius: 6px;
-    padding: 8px; white-space: pre-wrap; font-size: 12px; color: #94a3b8;
+    display: block;
+    width: 100vw;
+    height: 100vh;
+    touch-action: none;
@@ -39,27 +25 @@
-<h1>PocketJS playground</h1>
-<div class="row">
-  <select id="demo"></select>
-  <button id="run">Run</button>
-</div>
-<canvas id="screen" width="480" height="272"></canvas>
-<div class="row pad">
-  <button data-btn="0x0100">L</button>
-  <button data-btn="0x0080">&larr;</button>
-  <button data-btn="0x0010">&uarr;</button>
-  <button data-btn="0x0040">&darr;</button>
-  <button data-btn="0x0020">&rarr;</button>
-  <button data-btn="0x0200">R</button>
-  <button data-btn="0x4000">CROSS</button>
-  <button data-btn="0x2000">CIRCLE</button>
-  <button data-btn="0x8000">SQUARE</button>
-  <button data-btn="0x1000">TRIANGLE</button>
-  <button data-btn="0x0001">SELECT</button>
-  <button data-btn="0x0008">START</button>
-</div>
-<div id="keys">
-  keyboard: arrows = d-pad &middot; Enter / Z = CIRCLE &middot; X = CROSS &middot;
-  A = SQUARE &middot; S = TRIANGLE &middot; L / Q = LTRIGGER &middot; R / E = RTRIGGER &middot;
-  Shift = SELECT &middot; Space = START
-</div>
-<pre id="log"></pre>
-
+<canvas id="screen"></canvas>
@@ -67,9 +27 @@
-  import { mount, load, listDemos, pressVirtual } from "./engine.js";
-
-  const logEl = document.getElementById("log");
-  const demoEl = document.getElementById("demo");
-
-  function log(msg) {
-    logEl.textContent = (logEl.textContent + msg + "\n").slice(-8000);
-    logEl.scrollTop = logEl.scrollHeight;
-  }
+  import { mount, load, listDemos } from "./engine.js";
@@ -77,2 +29,2 @@
-  // FPS + memory are drawn on the canvas by the host (see host-web/hud.js).
-  await mount(document.getElementById("screen"), { onLog: log });
+  const canvas = document.getElementById("screen");
+  await mount(canvas, { onLog: console.log });
@@ -81,12 +33,4 @@
-  if (demos.length === 0) {
-    log("no bundles in dist/ — run: bun scripts/build.ts hero-main");
-  }
-  for (const d of demos) {
-    const opt = document.createElement("option");
-    opt.value = d.name;
-    opt.textContent = d.name + (d.mounts ? "" : "  (component only — won't mount)");
-    demoEl.appendChild(opt);
-  }
-  // Prefer a mounting entry (*-main) as the default selection.
-  const preferred = demos.find((d) => d.mounts) ?? demos[0];
-  if (preferred) demoEl.value = preferred.name;
+  const requested = new URLSearchParams(location.search).get("demo");
+  const selected = demos.find((demo) => demo.name === requested && demo.mounts)
+    ?? demos.find((demo) => demo.mounts);
+  if (!selected) throw new Error("No runnable PocketJS demo was found");
@@ -94,22 +38,2 @@
-  const run = () => load(demoEl.value);
-  document.getElementById("run").addEventListener("click", run);
-  demoEl.addEventListener("change", run);
-  const boot = new URLSearchParams(location.search).get("demo");
-  if (boot && demos.some((d) => d.name === boot)) demoEl.value = boot;
-  if (preferred || boot) await run();
-
-  // Virtual buttons: mouse + touch, release on leave.
-  for (const el of document.querySelectorAll("[data-btn]")) {
-    const bit = parseInt(el.dataset.btn, 16);
-    const set = (down) => (e) => {
-      e.preventDefault();
-      el.classList.toggle("held", down);
-      pressVirtual(bit, down);
-    };
-    el.addEventListener("mousedown", set(true));
-    el.addEventListener("mouseup", set(false));
-    el.addEventListener("mouseleave", set(false));
-    el.addEventListener("touchstart", set(true), { passive: false });
-    el.addEventListener("touchend", set(false));
-    el.addEventListener("touchcancel", set(false));
-  }
+  const error = await load(selected.name);
+  if (error) throw error;
diff --git a/host-web/wasm-ops.js b/host-web/wasm-ops.js
index ac0ce17..f6f1752 100644
--- a/host-web/wasm-ops.js
+++ b/host-web/wasm-ops.js
@@ -10 +10 @@
-// ui_render() returns the byte-exact RGBA8 480x272 framebuffer pointer;
+// ui_render() returns the RGBA8 framebuffer for the active logical viewport;
@@ -37,0 +38,3 @@ export async function createWasmUi(wasm) {
+  const viewport = { w: FB_W, h: FB_H };
+  const viewportListeners = new Set();
+
@@ -39,0 +43 @@ export async function createWasmUi(wasm) {
+    ex.ui_set_viewport(viewport.w, viewport.h);
@@ -58,0 +63,5 @@ export async function createWasmUi(wasm) {
+    __viewport: viewport,
+    __subscribeViewport(listener) {
+      viewportListeners.add(listener);
+      return () => viewportListeners.delete(listener);
+    },
@@ -106,0 +116,10 @@ export async function createWasmUi(wasm) {
+  function resize(width, height) {
+    width = integerInRange(width, "viewport width", 1, 8192);
+    height = integerInRange(height, "viewport height", 1, 8192);
+    if (viewport.w === width && viewport.h === height) return;
+    viewport.w = width;
+    viewport.h = height;
+    ex.ui_set_viewport(width, height);
+    for (const listener of viewportListeners) listener(viewport);
+  }
+
@@ -112 +131 @@ export async function createWasmUi(wasm) {
-      FB_W * scale * FB_H * scale * 4,
+      viewport.w * scale * viewport.h * scale * 4,
@@ -118,0 +138,2 @@ export async function createWasmUi(wasm) {
+    viewport,
+    resize,
diff --git a/src/devtools.ts b/src/devtools.ts
index 97e9952..8ffddb6 100644
--- a/src/devtools.ts
+++ b/src/devtools.ts
@@ -479 +479,3 @@ function serializeNode(node: NodeMirror): TreeNodeJson {
-  for (const child of node.children) {
+  const children = Array.isArray(node.children) ? node.children : [];
+  for (const child of children) {
+    if (!child || typeof child !== "object" || typeof child.id !== "number") continue;
@@ -489 +491,5 @@ function countNodes(node: NodeMirror): number {
-  for (const child of node.children) n += countNodes(child);
+  const children = Array.isArray(node.children) ? node.children : [];
+  for (const child of children) {
+    if (!child || typeof child !== "object" || typeof child.id !== "number") continue;
+    n += countNodes(child);
+  }
diff --git a/src/host.ts b/src/host.ts
index 0ac5df2..5421d23 100644
--- a/src/host.ts
+++ b/src/host.ts
@@ -168,0 +169,4 @@ export interface HostOps {
+  /** Mutable logical viewport published by resizable desktop/browser hosts. */
+  __viewport?: { w: number; h: number };
+  /** Subscribe to logical viewport changes. */
+  __subscribeViewport?(listener: (viewport: { w: number; h: number }) => void): () => void;
@@ -175 +179 @@ export function hostViewport(ops: HostOps): { w: number; h: number } | null {
-  return (ops as HostOps & { __viewport?: { w: number; h: number } }).__viewport ?? null;
+  return ops.__viewport ?? null;
diff --git a/src/index-vue-vapor.ts b/src/index-vue-vapor.ts
index b72905b..2e56248 100644
--- a/src/index-vue-vapor.ts
+++ b/src/index-vue-vapor.ts
@@ -5 +5 @@ import "./prelude.ts";
-import { detectHost, installFrameHandler, installHost, type HostOps } from "./host.ts";
+import { detectHost, hostViewport, installFrameHandler, installHost, type HostOps } from "./host.ts";
@@ -126,3 +126,6 @@ export function render(code: () => unknown, opts: RenderOptions = {}): () => voi
-  const appRoot = createLayer({
-    width: SCREEN_W,
-    height: SCREEN_H,
+  const viewport = hostViewport(host.ops);
+  const layerWidth = viewport?.w ?? SCREEN_W;
+  const layerHeight = viewport?.h ?? SCREEN_H;
+  let appRootStyle = {
+    width: layerWidth,
+    height: layerHeight,
@@ -130,4 +133,4 @@ export function render(code: () => unknown, opts: RenderOptions = {}): () => voi
-  });
-  const overlayRoot = createLayer({
-    width: SCREEN_W,
-    height: SCREEN_H,
+  };
+  let overlayRootStyle = {
+    width: layerWidth,
+    height: layerHeight,
@@ -140 +143,3 @@ export function render(code: () => unknown, opts: RenderOptions = {}): () => voi
-  });
+  };
+  const appRoot = createLayer(appRootStyle);
+  const overlayRoot = createLayer(overlayRootStyle);
@@ -144,0 +150,17 @@ export function render(code: () => unknown, opts: RenderOptions = {}): () => voi
+  const unsubscribeViewport = host.ops.__subscribeViewport?.((nextViewport) => {
+    const nextAppRootStyle = {
+      ...appRootStyle,
+      width: nextViewport.w,
+      height: nextViewport.h,
+    };
+    const nextOverlayRootStyle = {
+      ...overlayRootStyle,
+      width: nextViewport.w,
+      height: nextViewport.h,
+    };
+    setProp(appRoot, "style", nextAppRootStyle, appRootStyle);
+    setProp(overlayRoot, "style", nextOverlayRootStyle, overlayRootStyle);
+    appRootStyle = nextAppRootStyle;
+    overlayRootStyle = nextOverlayRootStyle;
+  });
+
@@ -163,0 +186 @@ export function render(code: () => unknown, opts: RenderOptions = {}): () => voi
+    unsubscribeViewport?.();
diff --git a/src/input-api.ts b/src/input-api.ts
index 446bd4f..075b6d7 100644
--- a/src/input-api.ts
+++ b/src/input-api.ts
@@ -9,0 +10 @@ export {
+  setCursorPosition,
diff --git a/src/input.ts b/src/input.ts
index c89d506..0750dd1 100644
--- a/src/input.ts
+++ b/src/input.ts
@@ -569,0 +570,15 @@ function disableCursor(): void {
+/** Position an enabled virtual cursor from a host-native pointer source.
+ *  Returns false when cursor mode has not been enabled. Coordinates use the
+ *  logical PocketJS viewport, matching hitTest and setCursorPos. */
+export function setCursorPosition(x: number, y: number): boolean {
+  if (!cursor || !Number.isFinite(x) || !Number.isFinite(y)) return false;
+  const width = cursor.vw || SCREEN_W;
+  const height = cursor.vh || SCREEN_H;
+  cursor.x = Math.min(Math.max(x, 0), width - 1);
+  cursor.y = Math.min(Math.max(y, 0), height - 1);
+  cursor.target = null;
+  cursor.gen = -1;
+  cursor.fresh = true;
+  return true;
+}
+
@@ -667,5 +682,7 @@ function cursorFrame(buttons: number, pressed: number, released: number): boolea
-  if (c.vw === 0) {
-    // First frame: latch the logical viewport and center an unplaced cursor.
-    const vp = hostViewport(ops);
-    c.vw = vp ? vp.w : SCREEN_W;
-    c.vh = vp ? vp.h : SCREEN_H;
+  const vp = hostViewport(ops);
+  const viewportWidth = vp ? vp.w : SCREEN_W;
+  const viewportHeight = vp ? vp.h : SCREEN_H;
+  if (c.vw !== viewportWidth || c.vh !== viewportHeight) {
+    const firstViewport = c.vw === 0;
+    c.vw = viewportWidth;
+    c.vh = viewportHeight;
@@ -674,0 +692,3 @@ function cursorFrame(buttons: number, pressed: number, released: number): boolea
+    } else if (!firstViewport) {
+      c.x = Math.min(c.x, c.vw - 1);
+      c.y = Math.min(c.y, c.vh - 1);
@@ -675,0 +696 @@ function cursorFrame(buttons: number, pressed: number, released: number): boolea
+    c.fresh = true;
diff --git a/src/native-tree.ts b/src/native-tree.ts
index d66301d..04651a1 100644
--- a/src/native-tree.ts
+++ b/src/native-tree.ts
@@ -366 +366 @@ function subtreeHasRetained(node: NodeMirror): boolean {
-  if (!node) return false;
+  if (!isNativeNode(node)) return false;
@@ -383 +383 @@ export function runSweep(): void {
-    if (!node) continue;
+    if (!isNativeNode(node)) continue;
diff --git a/wasm/src/lib.rs b/wasm/src/lib.rs
index 1c66ccc..9c2741e 100644
--- a/wasm/src/lib.rs
+++ b/wasm/src/lib.rs
@@ -25 +24,0 @@
-use pocketjs_core::spec::{SCREEN_H, SCREEN_W};
@@ -66,0 +66,6 @@ pub extern "C" fn ui_init(raster_density: u32) {
+/// Resize the logical viewport used for layout, hit-testing, clipping, and rendering.
+#[no_mangle]
+pub extern "C" fn ui_set_viewport(width: u32, height: u32) {
+    ui().set_viewport(width as f32, height as f32);
+}
+
@@ -256 +261,4 @@ fn render_at_scale(scale: u32) -> *const u8 {
-    let Some(width) = (SCREEN_W as usize).checked_mul(scale as usize) else {
+    let (logical_width, logical_height) = ui().viewport();
+    let logical_width = logical_width.round().clamp(1.0, 32000.0) as u32;
+    let logical_height = logical_height.round().clamp(1.0, 32000.0) as u32;
+    let Some(width) = (logical_width as usize).checked_mul(scale as usize) else {
@@ -259 +267 @@ fn render_at_scale(scale: u32) -> *const u8 {
-    let Some(height) = (SCREEN_H as usize).checked_mul(scale as usize) else {
+    let Some(height) = (logical_height as usize).checked_mul(scale as usize) else {
@@ -277,5 +285,8 @@ fn render_at_scale(scale: u32) -> *const u8 {
-        if scale == 1 {
-            raster::render(u_ref, &(*dl).words, &mut FRAMEBUFFER);
-        } else {
-            raster::render_scaled(u_ref, &(*dl).words, &mut FRAMEBUFFER, scale);
-        }
+        raster::render_viewport_scaled(
+            u_ref,
+            &(*dl).words,
+            &mut FRAMEBUFFER,
+            logical_width,
+            logical_height,
+            scale,
+        );
